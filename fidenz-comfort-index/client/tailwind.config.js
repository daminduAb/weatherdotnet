/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}",
  ],
  darkMode: 'class', // enables dark: variants when a `dark` class is present on <html>
  theme: {
    extend: {},
  },
  plugins: [],
}